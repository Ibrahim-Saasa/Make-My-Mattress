import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hindustanmattress.makemymattress',
  appName: 'Make My Mattress',
  webDir: 'dist'
};

export default config;
